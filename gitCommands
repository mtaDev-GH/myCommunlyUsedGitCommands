git init

git status

git add .

git commit -m "<message>"

(git log)

git log

git log --all --oneline --graph

git log --graph --all --decorate --simplify-by-decoration

git log --graph --all --decorate --oneline

***

git branch

git checkout <branchName>

git branch <newBranchName>

git checkout -b <newBranchName>

git remote

git remote add <localLabelForRemoteRepo> <url>

git push

git push -u <localLabelForRemoteRepo> <remoteBranchName>

git branch -u <localLabelForRemoteRepo>/<remoteBranchName>

git branch -vv

git merge <branchToMergeFrom>

git fetch --all

git restore .

git checkout .

git tag

git tag -a <tagName> -m "message"

git show <tagName>

git remote rename <old> <new>

git remote remove <name>

git branch --delete --remotes <remote>/<branch>

# Delete multiple obsolete remote-tracking branches
git fetch <remote> --prune

git push origin --tags

git push origin <tagName>

git branch --all

git pull --all

git tag --delete <tagname>

git branch -m <oldname> <newname>

git branch -m <newname>

git push --delete <origin> <tagname>

git branch --set-upstream <remote-branch>
